@model IEnumerable<scm.Models.resPrepMaterial>

@{
    ViewBag.Title = "Preparation Form";
}

<h2>Preparation Details</h2>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Qty)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.resPreparation.dtPrepared)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.scItem.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.scStoreBin.Code)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Qty)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.resPreparation.dtPrepared)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.scItem.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.scStoreBin.Code)
        </td>
        <td>
            @Html.ActionLink("Edit", "EditItem", new { id=item.Id }) |
            @Html.ActionLink("Delete", "DeleteItem", new { id=item.Id })
        </td>
    </tr>
}

    <tr>
        <td colspan="5">
            @Html.ActionLink("Add Item", "CreateItem")
        </td>
    </tr>
</table>

<div>
    @Html.ActionLink("Back to List", "Index")
</div>


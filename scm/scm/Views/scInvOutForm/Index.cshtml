@model IEnumerable<scm.Models.scInvOutHdr>

@{
    ViewBag.Title = "Index";
}

<h2>Inventory Movement</h2>
Inventory Out

<p>
    @Html.ActionLink("Create New Transaction", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.dtOut)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Remarks)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.dtOut)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Remarks)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>

@{Html.RenderPartial("PartialView_LowLevelItems", (List<scm.Models.EntItems>)ViewBag.LowLevelItems);}
<p>
    @Html.ActionLink("Home", "Index", "Home") |
    @Html.ActionLink("Movement", "Index", "scInvOutForm") |
    @Html.ActionLink("Repack", "index", "resPreparationForm")
</p>

@model IEnumerable<scm.Models.scRcvHdr>

@{
    ViewBag.Title = "Index";
}

<h2>Receiving</h2>
List

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.dtRcv)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Remarks)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.scSupplier.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.dtRcv)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Remarks)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.scSupplier.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>

@{Html.RenderPartial("PartialView_ItemStatus", (List<scm.Models.EntOrderStatus>)ViewBag.ItemStatus); }

<p>
    @Html.ActionLink("Home", "Index", "Home") |
    @Html.ActionLink("Items", "ItemInventory", "scItems") |
    @Html.ActionLink("Storage Bins", "StoreBin", "scStoreBins") |
    @Html.ActionLink("Request", "Index", "scPrForm") |
    Receiving
</p>
